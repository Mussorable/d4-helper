import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      main: "#0b1354",
    },
  },
  typography: {
    fontFamily: ["Poppins", "sans-serif"].join(","),
  },
});
